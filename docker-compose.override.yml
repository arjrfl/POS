# Dev-only overrides — auto-loaded by `docker compose up`.
# Do NOT deploy this file to the production server.
services:
  backend:
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
